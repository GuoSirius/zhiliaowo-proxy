{
  "name": "zhiliaowo-monorepo",
  "private": true,
  "version": "2.1.0",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node scripts/clean-ports.cjs && pnpm -r --parallel run dev",
    "dev:proxy": "pnpm --filter zhiliaowo-proxy dev",
    "dev:admin": "pnpm --filter @zhiliaowo/admin dev",
    "dev:h5": "pnpm --filter @zhiliaowo/h5 dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm --filter @zhiliaowo/core build && pnpm -r typecheck",
    "test": "pnpm -r test",
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "changelog": "changelogen",
    "release": "node scripts/release.mjs",
    "upgrade": "pnpm up -r --latest"
  },
  "devDependencies": {
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "changelogen": "catalog:",
    "husky": "catalog:",
    "node-xlsx": "^0.21.0"
  }
}
